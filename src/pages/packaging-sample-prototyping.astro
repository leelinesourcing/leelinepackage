---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

/* The other two services live in-repo; this page is the third service. */
const CHILD = {
  structure: '/structural-engineering-design/',
  artwork: '/packaging-artwork-design/',
};

/* What you get first — the conversion hook. These are the only place the "first
   deliverable" facts live, so nothing repeats. (Quote / white / printed times,
   revision count.) Distinct from the services-hub stats. */
const first = [
  { v: '24\u201348h', l: 'to a quote & material suggestion' },
  { v: '3\u20135 days', l: 'to a white dummy' },
  { v: '7\u201310 days', l: 'to a printed prototype' },
  { v: '2', l: 'free revisions per sample' },
];

/* The four sample types, each with what it looks like and what it validates.
   `icon` picks the inline identity glyph; `tone` tints the placeholder slot. */
const samples = [
  {
    n: '01',
    icon: '3d',
    tone: 'brass',
    name: '3D Sample',
    key: 'in-screen model',
    tag: 'See the box before a sheet is cut.',
    looks: ['Rotate it and open it from any angle', 'No physical sheet is printed', 'A link to send for client sign-off'],
    validates: ['Structure reads correctly on screen', 'Proportions and openings look right', 'Catch a dieline error before tooling'],
    media: 'A rotating, openable 3D box model on screen \u2014 the interface, not a beautified render',
  },
  {
    n: '02',
    icon: 'white',
    tone: 'kraft',
    name: 'White Dummy',
    key: 'unprinted rigid box prototype',
    tag: 'Hold the structure, check the fit.',
    looks: ['No print \u2014 board, flute and fold only', 'Corrugation and tuck flaps visible', 'Magnetic closure, tabs and assembly on hand'],
    validates: ['Product fits, never floats or jams', 'Assembly is easy to repeat on a line', 'Wall thickness and weight feel right'],
    media: 'A plain white unprinted box dummy with a product being fitted by hand \u2014 the hand checking the hold, not posing',
  },
  {
    n: '03',
    icon: 'colour',
    tone: 'clay',
    name: 'Digital Colour Sample',
    key: 'digital printed carton prototype',
    tag: 'See the artwork on the real board.',
    looks: ['Digitally printed artwork', 'Registration crosses and cut edges visible', 'Whether the colour reads dull or true'],
    validates: ['Artwork sits inside the safe lines', 'No trapped text or bleeding', 'Screen colour holds on coated board'],
    media: 'A digitally printed carton prototype showing registration marks, cut edges and the printed artwork',
  },
  {
    n: '04',
    icon: 'prod',
    tone: 'forest',
    name: 'Production-Line Sample',
    key: 'production sample vs mockup',
    tag: 'The real material, the real process.',
    looks: ['Same material, print and finish as the run', 'Foil, lamination and emboss in place', 'A transit-tested version beside it'],
    validates: ['Foil and finish hold in the hand', 'The box survives the courier route', 'Cost per unit is locked before bulk'],
    media: 'A production-line sample with foil and lamination, shown beside its half after a simulated-transit test',
  },
];

/* Approval scene \u2014 the sign-off points that make it feel like a real factory. */
const signoff = [
  { k: 'Colour reference', v: 'Pantone / CMYK proof stapled to the sheet' },
  { k: 'Board & flute', v: 'The exact grade the line will run' },
  { k: 'Dimensions & dieline', v: 'Locked to the signed template' },
  { k: 'Finish map', v: 'Foil, varnish, emboss \u2014 spot for spot' },
];

/* Material durability \u2014 the tests the sample goes through before a run. */
const tests = [
  { t: 'Weight-bearing', d: 'Rated to the stack height the box will meet in a warehouse or store.' },
  { t: 'Drop & impact', d: 'Dropped flat, on edge and corner \u2014 the product stays clear of the board.' },
  { t: 'Thickness & rigidity', d: 'Caliper and stiffness measured against the specified board, not assumed.' },
  { t: 'Print & colour fastness', d: 'Ink adhesion and colour held after rub and light, before the first use.' },
  { t: 'Environmental simulation', d: 'Humidity and temperature cycling \u2014 the conditions the route adds.' },
];

/* Process \u2014 the path from brief to approved sample. */
const process = [
  { t: 'Send the brief', d: 'Artwork in AI, PDF or EPS, or the product size, weight and target price.' },
  { t: 'Check & quote', d: 'A feasibility, material and print check, then a price and a suggested build.' },
  { t: 'Cut a sample', d: 'A white dummy for fit first, then a printed prototype for colour and finish.' },
  { t: 'Revise & approve', d: 'Feedback on fit, colour or finish, then a sign-off against the approved sample and notes.' },
  { t: 'Run the line', d: 'Tooling is cut and production builds to the signed sample \u2014 colour, board and size locked.' },
];

/* FAQ \u2014 sample-led, no overlap with structure / artwork pages. */
const faqs = [
  { q: 'Why order a packaging sample?', a: 'A sample tests what a render can\u2019t \u2014 transit durability, true colour, and how easily a team assembles the box. A mistake found here costs a sample; the same mistake in production costs a run.' },
  { q: 'How does the sample process start?', a: 'The design file or a brief is checked first \u2014 print and material feasibility, a suggested build, then the first sample is cut.' },
  { q: 'Which materials can a sample use?', a: 'Corrugated, greyboard for rigid boxes, kraft, coated carton, plus finishes such as foil stamping and embossing. A sample is built to the exact material and finish of the production order.' },
  { q: 'Are samples revised if they are not right?', a: 'Yes. Each sample order carries up to two free revisions, made against feedback on fit, colour or finish before it is approved for the run.' },
  { q: 'What if the sample doesn\u2019t match the spec?', a: 'Where variance is on the supplier side \u2014 colour drift or material not matching the order \u2014 it is remade at no cost, or refunded in full.' },
];

const seo = {
  title: 'Packaging Sample & Prototyping: 4 Samples, Proof Before Bulk',
  description: 'Pick a sample to de-risk the run: a white dummy for fit, a digital printed prototype for colour and a production-line sample with foil \u2014 real proof on the board',
  keywords: 'packaging sample prototyping, white dummy packaging sample, digital printed carton prototype, production sample vs mockup, packaging sample approval, unprinted rigid box prototype',
  canonical: 'https://www.leelinepackage.com/packaging-sample-prototyping/',
  ogImage: 'https://pub-f65f4d4fa9664133a4ceca1f5d43e24d.r2.dev/services/sample.webp',
};

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Service',
      name: 'Packaging Sample & Prototyping',
      description: seo.description,
      serviceType: 'Custom packaging sample prototyping and proofing',
      provider: {
        '@type': 'Organization',
        name: 'LeelinePackage',
        url: 'https://www.leelinepackage.com/',
        areaServed: 'Worldwide',
      },
      areaServed: 'Worldwide',
      offers: { '@type': 'Offer', price: 'Free quote', priceCurrency: 'USD' },
      aggregateRating: { '@type': 'AggregateRating', ratingValue: '4.9', reviewCount: '320' },
    },
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://www.leelinepackage.com/' },
        { '@type': 'ListItem', position: 2, name: 'Services', item: 'https://www.leelinepackage.com/services/' },
        { '@type': 'ListItem', position: 3, name: 'Packaging Sample & Prototyping' },
      ],
    },
    {
      '@type': 'FAQPage',
      mainEntity: faqs.map((f) => ({
        '@type': 'Question', name: f.q,
        acceptedAnswer: { '@type': 'Answer', text: f.a },
      })),
    },
  ],
};

const idIcon = (name) => {
  const paths = {
    '3d': ['M12 3 3 8v8l9 5 9-5V8l-9-5Z', 'M3 8l9 5 9-5M12 13v8', 'M18.5 4.6a7.4 7.4 0 0 1 2 7.4'],
    white: ['M4 9l8-4 8 4v8l-8 4-8-4V9Z', 'M4 9l8 4 8-4M12 13v8', 'M6.5 5l-.5 2.4M17.5 5l.5 2.4'],
    colour: ['M4 9l8-4 8 4v7l-8 4-8-4V9Z', 'M4 9l8 4 8-4M12 13v7'],
    prod: ['M4 9l8-4 8 4v8l-8 4-8-4V9Z', 'M4 9l8 4 8-4M12 13v8', 'M17 3.5l3.2 1.6 1.2 3.2-1.2 3.2-3.2 1.6-3.2-1.6-1.2-3.2 1.2-3.2 3.2-1.6Z'],
  };
  return paths[name] || [];
};
const colourDots = [[8.5, 13.5], [13, 15.5], [17.5, 12.5]];
---

<Layout
  title={seo.title}
  description={seo.description}
  keywords={seo.keywords}
  canonical={seo.canonical}
  ogType="website"
  ogImage={seo.ogImage}
  jsonLd={JSON.stringify(jsonLd)}
>
  <main id="top">
    <Navbar />

    <!-- ============ HERO (split · single sample image) ============ -->
    <section class="sp-hero">
      <div class="wrap sp-hero-grid">
        <div class="sp-hero-copy">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Hands-on packaging</span>
          <h1 class="sp-hero-title">Packaging Sample &amp; Prototyping: <em>De-Risk Your Mass Production Before Scaling</em></h1>
          <p class="sp-hero-sub">The sample is the cheapest place to find a costly mistake. Approve the board, finish and fit on one prototype, so unit cost, reprint risk and damage returns are settled before the first bulk run &mdash; MOQ 500, on your lead time.</p>
          <div class="sp-hero-cta" data-reveal="up">
            <a class="btn btn-gold" href="/contact" target="_blank" rel="noopener">
              Request a Sample
              <svg class="btn-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </a>
          </div>
        </div>

        <div class="sp-hero-visual" data-reveal="right">
          <figure class="sp-media sp-hero-ph">
            <svg class="sp-media-ico sp-hero-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              {idIcon('3d').map((d) => <path d={d} />)}
            </svg>
            <span class="sp-tag">Sample photo</span>
            <figcaption class="sp-media-cap">
              <span class="sp-media-k">Four samples, one structure</span>
              <span class="sp-media-s">3D &middot; white dummy &middot; digital colour &middot; production-line</span>
            </figcaption>
          </figure>
        </div>
      </div>
    </section>

    <!-- ============ WHAT YOU GET FIRST (forest strip) ============ -->
    <section class="sp-first" aria-label="What you get first">
      <div class="wrap sp-first-row">
        {first.map((s) => (
          <div class="sp-first-cell">
            <strong>{s.v}</strong>
            <span>{s.l}</span>
          </div>
        ))}
      </div>
    </section>

    <!-- ============ THE FOUR SAMPLES (alternating identity rows) ============ -->
    <section class="sp-id" id="samples">
      <div class="wrap">
        <div class="section-head">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Four samples</span>
          <h2 class="section-title" data-reveal="up">What Each Sample Looks Like, <em>and What It Proves</em></h2>
          <p class="section-sub" data-reveal="up">Every sample is built to the same structure, so the step between the four is the material and the finish &mdash; not a different box. Here is what each one shows and the risk it takes off.</p>
        </div>

        <div class="sp-id-list">
          {samples.map((s, i) => (
            <article class={"sp-id-row" + (i % 2 ? ' sp-id-row--rev' : '')} data-reveal="up">
              <figure class={"sp-media sp-id-ph sp-media--" + s.tone}>
                {s.icon === 'colour' ? (
                  <svg class="sp-media-ico sp-id-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    {idIcon(s.icon).map((d) => <path d={d} />)}
                    {colourDots.map(([x, y]) => <circle cx={x} cy={y} r="1.2" />)}
                  </svg>
                ) : (
                  <svg class="sp-media-ico sp-id-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    {idIcon(s.icon).map((d) => <path d={d} />)}
                  </svg>
                )}
                <span class="sp-tag">Sample photo</span>
              </figure>

              <div class="sp-id-body">
                <span class="sp-id-eyebrow">{s.n} &middot; {s.key}</span>
                <h3 class="sp-id-name">{s.name}</h3>
                <p class="sp-id-tagline">{s.tag}</p>

                <div class="sp-id-cols">
                  <div class="sp-id-col">
                    <h4>What it looks like</h4>
                    <ul class="sp-id-list-col">
                      {s.looks.map((l) => <li><svg class="sp-bullet" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>{l}</li>)}
                    </ul>
                  </div>
                  <div class="sp-id-col">
                    <h4>What it validates</h4>
                    <ul class="sp-id-list-col sp-id-list-col--check">
                      {s.validates.map((l) => <li><svg class="sp-bullet" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 12.5 9.5 18 20 6" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></svg>{l}</li>)}
                    </ul>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- ============ APPROVAL SCENE (forest) ============ -->
    <section class="sp-approve" id="approval">
      <div class="wrap sp-approve-grid">
        <figure class="sp-media sp-approve-ph" data-reveal="left" aria-hidden="true">
          <span class="sp-note sp-note--ok">Approved</span>
          <span class="sp-note sp-note--rev">Revise &middot; fit</span>
          <svg class="sp-media-ico sp-approve-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 9l8-4 8 4v8l-8 4-8-4V9Z"/><path d="M4 9l8 4 8-4M12 13v8"/></svg>
          <figcaption class="sp-media-cap"><span class="sp-media-k">Sample approval</span><span class="sp-media-s">Signed, dated, stamped</span></figcaption>
        </figure>

        <div class="sp-approve-body" data-reveal="up">
          <span class="eyebrow eyebrow--light"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Packaging sample approval</span>
          <h2 class="sp-approve-title" data-reveal="up">Sign It Off <em>Like a Real Factory</em></h2>
          <p class="sp-approve-sub" data-reveal="up">The approved sample is the reference the whole run is built to. Notes and a signature replace a &ldquo;yes, looks good&rdquo; &mdash; so the line, the printer and QC all work from the same sheet.</p>
          <ul class="sp-signoff" data-reveal="up">
            {signoff.map((s) => (
              <li><span class="sp-signoff-k">{s.k}</span><span class="sp-signoff-v">{s.v}</span></li>
            ))}
          </ul>
          <p class="sp-guarantee" data-reveal="up">If the sample drifts from the spec &mdash; colour variance or a material mismatch &mdash; it is remade at no cost, or refunded in full.</p>
        </div>
      </div>
    </section>

    <!-- ============ MATERIAL DURABILITY ============ -->
    <section class="sp-test" id="testing">
      <div class="wrap sp-test-grid">
        <div class="sp-test-head" data-reveal="up">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Material &amp; durability</span>
          <h2 class="section-title">Test the Material, <em>Not the Mockup</em></h2>
          <p class="section-sub">A sample is built to the material the order ships &mdash; corrugated, greyboard, kraft or coated carton, plus eco and food-contact grades and finishes like foil and emboss. That makes it a true proxy for the run, not a stand-in.</p>
          <figure class="sp-media sp-test-ph">
            <svg class="sp-media-ico sp-test-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5h16M4 9h16M4 13h16"/><path d="M12 17v3M9 18l3 3 3-3"/></svg>
            <span class="sp-tag">Durability photo</span>
            <figcaption class="sp-media-cap"><span class="sp-media-k">Test in progress</span><span class="sp-media-s">A board section under a stack-load test</span></figcaption>
          </figure>
        </div>

        <ol class="sp-test-list">
          {tests.map((t, i) => (
            <li class="sp-test-row" data-reveal="up" style={`--d:${i * 0.06}s`}>
              <span class="sp-test-num">0{i + 1}</span>
              <div class="sp-test-body">
                <h3>{t.t}</h3>
                <p>{t.d}</p>
              </div>
            </li>
          ))}
        </ol>
      </div>
    </section>

    <!-- ============ PROCESS (step-by-step) ============ -->
    <section class="sp-process" id="how">
      <div class="wrap">
        <div class="section-head section-head--center">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>From brief to run</span>
          <h2 class="section-title" data-reveal="up">A Short Path to Something <em>You Can Hold</em></h2>
          <p class="section-sub" data-reveal="up">A transparent route with a review at each step &mdash; so a sample is a decision, not a gamble.</p>
        </div>

        <ol class="sp-proc-list">
          {process.map((p, i) => (
            <li class="sp-proc-step" data-reveal="up" style={`--d:${(i % 2) * 0.06}s`}>
              <span class="sp-proc-num">{p.t.slice(0, 1)}</span>
              <div class="sp-proc-body"><h3>{p.t}</h3><p>{p.d}</p></div>
            </li>
          ))}
        </ol>
      </div>
    </section>

    <!-- ============ WHERE IT FITS (cross-links) ============ -->
    <section class="sp-cross" id="next">
      <div class="wrap">
        <div class="section-head section-head--center">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Where this fits</span>
          <h2 class="section-title" data-reveal="up">The Sample Sits <em>Between Two Services</em></h2>
          <p class="section-sub" data-reveal="up">The structure is decided behind it, the artwork sits on it. The sample is the proof between the two.</p>
        </div>

        <div class="sp-next-grid">
          <a class="sp-next-card" data-reveal="up" href={CHILD.structure}>
            <span class="sp-next-kicker">Behind the sample</span>
            <h4>Structural engineering</h4>
            <p>Pick the structure, board and dieline that fit the channel and the route.</p>
            <span class="text-link">Go to structural engineering <svg class="arrow" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
          </a>
          <a class="sp-next-card" data-reveal="up" style={`--d:0.1s`} href={CHILD.artwork}>
            <span class="sp-next-kicker">On the sample</span>
            <h4>Packaging artwork design</h4>
            <p>Put the artwork, colour and finishes on the real dieline before the proof.</p>
            <span class="text-link">Go to artwork design <svg class="arrow" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
          </a>
        </div>
      </div>
    </section>

    <!-- ============ FAQ (centred head) ============ -->
    <section class="sp-faq" id="faq">
      <div class="wrap">
        <div class="section-head section-head--center">
          <span class="eyebrow"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>FAQ</span>
          <h2 class="section-title" data-reveal="up">Before You <em>Order a Sample</em></h2>
          <p class="section-sub" data-reveal="up">The questions that come up before a proof. Structure and artwork detail live on their own pages.</p>
        </div>

        <div class="sp-faq-list">
          {faqs.map((f) => (
            <details class="sp-faq-item" data-reveal="up">
              <summary><span class="sp-faq-num">Q</span>{f.q}</summary>
              <div class="sp-faq-a"><p>{f.a}</p></div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- ============ FINAL CTA (dark) ============ -->
    <section class="sp-final" id="get-started">
      <div class="wrap sp-final-inner">
        <span class="eyebrow eyebrow--light"><svg class="eyebrow-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 4c-8.5 0-15 5.2-15.4 13 .6-3 2.7-5.6 6-6.7C15.4 8.8 19 7.5 20 4z" fill="currentColor"/></svg>Request a sample</span>
        <h2 class="sp-final-title" data-reveal="up">Get a Sample in Your Hands, <em>Before You Commit</em></h2>
        <p class="sp-final-sub" data-reveal="up">Send the artwork or a brief, and a quote and a material recommendation come back before the first sample is cut.</p>
        <div class="section-cta" data-reveal="up">
          <a class="btn btn-gold" href="/contact" target="_blank" rel="noopener">
            Request a Sample
            <svg class="btn-leaf" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </a>
        </div>
      </div>
    </section>

    <Footer />
  </main>
</Layout>

<style>
  /* ============ HERO (split \u00b7 2\u00d72 grid) ============ */
  .sp-hero { background: var(--ivory); padding: calc(clamp(7rem, 14vw, 9rem) + 0px) 0 clamp(3.5rem, 7vw, 5rem); position: relative; overflow: hidden; }
  .sp-hero::before { content: ''; position: absolute; inset: 0; pointer-events: none; background: radial-gradient(58% 52% at 80% 16%, rgba(201, 168, 124, 0.16), transparent 70%); }
  .sp-hero-grid { position: relative; z-index: 1; display: grid; grid-template-columns: 0.95fr 1.05fr; gap: clamp(2rem, 5vw, 4rem); align-items: center; }
  .sp-hero-title { font-family: var(--font-heading); font-weight: 700; font-size: clamp(2.4rem, 5.5vw, 4.1rem); line-height: 1.02; letter-spacing: -0.03em; color: var(--color-brand-text); margin: 1.2rem 0 0; max-width: 19ch; text-wrap: balance; }
  .sp-hero-title em { font-style: italic; color: var(--brass); }
  .sp-hero-sub { font-size: clamp(1.02rem, 1.7vw, 1.16rem); line-height: 1.7; color: var(--color-brand-muted); max-width: 52ch; margin: 1.4rem 0 0; }
  .sp-hero-cta { display: flex; flex-wrap: wrap; gap: 0.9rem; margin-top: 2.1rem; }

  .sp-hero-visual { display: flex; align-items: center; }
  .sp-hero-ph { margin: 0; width: 100%; aspect-ratio: 4 / 3; }
  .sp-hero-ico { width: 64px; height: 64px; }

  /* placeholder media slot \u2014 shared by hero cards, identity rows, approval */
  .sp-media { position: relative; border: 1.5px dashed rgba(176, 141, 76, 0.5); border-radius: 1.1rem; background: repeating-linear-gradient(45deg, rgba(201, 168, 124, 0.1) 0 12px, transparent 12px 26px), var(--color-brand-surface); display: grid; place-items: center; color: var(--brass); overflow: hidden; }
  .sp-media-ico { width: 42px; height: 42px; opacity: 0.85; }
  .sp-media-cap { position: absolute; left: 0; right: 0; bottom: 0; padding: 0.8rem 0.85rem; background: linear-gradient(180deg, transparent, rgba(20, 38, 27, 0.62)); color: var(--ivory); display: flex; flex-direction: column; gap: 0.15rem; }
  .sp-media-k { font-family: var(--font-body); font-size: 0.68rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: #E3C29A; }
  .sp-media-k b { color: #FFFFFF; font-weight: 700; }
  .sp-media-s { font-size: 0.82rem; font-weight: 600; line-height: 1.3; }
  .sp-tag { position: absolute; left: 0.7rem; top: 0.7rem; font-size: 0.66rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--forest-deep); background: rgba(245, 240, 229, 0.92); border: 1px solid rgba(31, 59, 44, 0.16); border-radius: 999px; padding: 0.28rem 0.6rem; }
  .sp-media--brass { border-color: rgba(176, 141, 76, 0.55); }
  .sp-media--kraft { border-color: rgba(201, 168, 124, 0.65); }
  .sp-media--clay { border-color: rgba(176, 141, 76, 0.5); }
  .sp-media--forest { border-color: rgba(46, 79, 61, 0.5); color: var(--forest-light); }

  /* ============ WHAT YOU GET FIRST ============ */
  .sp-first { background: linear-gradient(180deg, var(--forest-light) 0%, var(--color-forest) 100%); padding: 1.8rem 0; }
  .sp-first-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
  .sp-first-cell { text-align: center; }
  .sp-first-cell strong { font-family: var(--font-heading); font-weight: 500; font-size: clamp(1.5rem, 2.8vw, 2.1rem); color: var(--brass-soft); display: block; line-height: 1; }
  .sp-first-cell span { font-size: 0.78rem; line-height: 1.4; color: rgba(245, 240, 229, 0.82); display: block; margin-top: 0.35rem; }

  /* ============ THE FOUR SAMPLES ============ */
  .sp-id { background: var(--color-brand-surface); padding: clamp(4rem, 9vw, 8rem) 0; }
  .sp-id-list { display: flex; flex-direction: column; gap: clamp(1.6rem, 4vw, 2.6rem); }
  .sp-id-row { display: grid; grid-template-columns: 0.9fr 1.1fr; gap: clamp(1.8rem, 4vw, 3.2rem); align-items: center; }
  .sp-id-row--rev { direction: rtl; }
  .sp-id-row--rev > * { direction: ltr; }
  .sp-id-ph { aspect-ratio: 4 / 3; margin: 0; }
  .sp-id-ico { width: 48px; height: 48px; }
  .sp-id-body { display: flex; flex-direction: column; align-items: flex-start; }
  .sp-id-row--rev .sp-id-body { align-items: flex-end; text-align: right; }
  .sp-id-eyebrow { font-family: var(--font-body); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.18em; text-transform: uppercase; color: var(--brass); }
  .sp-id-name { font-family: var(--font-heading); font-weight: 600; font-size: clamp(1.7rem, 3.4vw, 2.5rem); letter-spacing: -0.02em; color: var(--color-brand-text); margin: 0.55rem 0 0.35rem; }
  .sp-id-tagline { font-size: 1.08rem; line-height: 1.6; color: var(--color-brand-primary); margin: 0 0 1.3rem; }
  .sp-id-cols { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(1rem, 3vw, 2rem); width: 100%; }
  .sp-id-col h4 { font-family: var(--font-body); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--forest-deep); margin: 0 0 0.8rem; }
  .sp-id-list-col { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.7rem; }
  .sp-id-list-col li { display: flex; align-items: flex-start; gap: 0.6rem; font-size: 0.96rem; line-height: 1.5; color: var(--color-brand-muted); }
  .sp-id-list-col--check li { color: var(--color-brand-primary); }
  .sp-bullet { width: 18px; height: 18px; flex: 0 0 auto; color: var(--brass); margin-top: 0.05rem; }

  /* ============ APPROVAL (forest) ============ */
  .sp-approve { background: linear-gradient(180deg, var(--forest-light) 0%, var(--color-forest) 100%); color: var(--ivory); padding: clamp(4rem, 9vw, 8rem) 0; }
  .sp-approve-grid { display: grid; grid-template-columns: 1fr 1.1fr; gap: clamp(2rem, 5vw, 4rem); align-items: center; }
  .sp-approve-ph { aspect-ratio: 4 / 3; margin: 0; border-color: rgba(245, 240, 229, 0.3); color: #E3C29A; }
  .sp-approve-ico { width: 56px; height: 56px; }
  .sp-approve-ph .sp-media-cap { background: linear-gradient(180deg, transparent, rgba(20, 38, 27, 0.7)); }
  .sp-note { position: absolute; font-family: var(--font-heading); font-weight: 600; padding: 0.5rem 0.8rem; border-radius: 0.5rem; box-shadow: 0 10px 22px -10px rgba(20, 38, 27, 0.6); }
  .sp-note--ok { top: 12%; right: 10%; transform: rotate(6deg); background: #F5F0E5; color: var(--forest-deep); }
  .sp-note--rev { bottom: 16%; left: 8%; transform: rotate(-5deg); background: rgba(228, 181, 77, 0.95); color: var(--forest-deep); }
  .sp-approve .section-title { color: #FFFFFF; }
  .sp-approve .section-title em { color: #E3C29A; }
  .sp-approve .section-sub, .sp-approve-sub { color: rgba(245, 240, 229, 0.85); }
  .sp-approve-title { font-family: var(--font-heading); font-weight: 500; font-size: clamp(2.2rem, 5vw, 3.6rem); line-height: 1.04; letter-spacing: -0.02em; color: #FFFFFF; text-shadow: 0 2px 24px rgba(20, 38, 27, 0.35); margin: 1.2rem 0 0; text-wrap: balance; }
  .sp-approve-title em { font-style: italic; color: #E3C29A; }
  .sp-approve-sub { font-size: 1.05rem; line-height: 1.7; max-width: 52ch; margin: 1.3rem 0 0; }
  .sp-signoff { list-style: none; margin: 1.8rem 0 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: 0.9rem 1.4rem; }
  .sp-signoff li { border-top: 1px solid rgba(245, 240, 229, 0.18); padding-top: 0.6rem; }
  .sp-signoff-k { display: block; font-family: var(--font-body); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--brass-soft); }
  .sp-signoff-v { display: block; font-size: 0.95rem; line-height: 1.5; color: rgba(245, 240, 229, 0.95); margin-top: 0.25rem; }
  .sp-guarantee { font-size: 1rem; line-height: 1.7; color: rgba(245, 240, 229, 0.92); max-width: 56ch; margin: 1.6rem 0 0; padding: 1rem 1.1rem; border: 1px solid rgba(228, 181, 77, 0.4); border-radius: 1rem; background: rgba(228, 181, 77, 0.08); }

  /* ============ MATERIAL DURABILITY ============ */
  .sp-test { background: var(--ivory); padding: clamp(4rem, 9vw, 8rem) 0; }
  .sp-test-grid { display: grid; grid-template-columns: 0.85fr 1.15fr; gap: clamp(2rem, 5vw, 4rem); align-items: start; }
  .sp-test-head { position: sticky; top: 6rem; }
  .sp-test-ph { margin: clamp(1.8rem, 4vw, 2.4rem) 0 0; aspect-ratio: 4 / 3; }
  .sp-test-ico { width: 48px; height: 48px; }
  .sp-test-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; }
  .sp-test-row { display: flex; gap: 1.2rem; align-items: flex-start; padding: clamp(1.2rem, 2.6vw, 1.6rem) 0; border-top: 1px solid rgba(31, 59, 44, 0.12); }
  .sp-test-row:last-child { border-bottom: 1px solid rgba(31, 59, 44, 0.12); }
  .sp-test-num { font-family: var(--font-heading); font-weight: 600; font-size: clamp(1.4rem, 2.6vw, 1.9rem); line-height: 1; color: var(--brass); flex: 0 0 auto; min-width: 2ch; padding-top: 0.1rem; }
  .sp-test-body h3 { font-family: var(--font-heading); font-weight: 600; font-size: 1.35rem; letter-spacing: -0.01em; color: var(--color-brand-text); margin: 0 0 0.4rem; }
  .sp-test-body p { font-size: 0.98rem; line-height: 1.6; color: var(--color-brand-muted); max-width: 52ch; margin: 0; }

  /* ============ PROCESS ============ */
  .sp-process { background: var(--color-brand-surface); padding: clamp(4rem, 9vw, 8rem) 0; }
  .sp-proc-list { list-style: none; margin: clamp(2rem, 4vw, 3rem) auto 0; padding: 0; max-width: 760px; position: relative; }
  .sp-proc-list::before { content: ''; position: absolute; left: 24px; top: 8px; bottom: 8px; width: 1.5px; background: linear-gradient(180deg, var(--brass) 0%, rgba(176, 141, 76, 0.35) 100%); }
  .sp-proc-step { position: relative; display: flex; gap: clamp(1.2rem, 3vw, 1.8rem); align-items: flex-start; padding: 0 0 clamp(1.8rem, 4vw, 2.4rem) 0; }
  .sp-proc-step:last-child { padding-bottom: 0; }
  .sp-proc-num { flex: 0 0 48px; width: 48px; height: 48px; border-radius: 50%; display: grid; place-items: center; background: var(--ivory); border: 1px solid var(--brass); color: var(--forest-deep); font-family: var(--font-heading); font-weight: 600; font-size: 1.05rem; position: relative; z-index: 1; }
  .sp-proc-body { padding: 0.3rem 0 0; }
  .sp-proc-body h3 { font-family: var(--font-heading); font-weight: 600; font-size: 1.45rem; letter-spacing: -0.01em; color: var(--color-brand-text); margin: 0 0 0.35rem; }
  .sp-proc-body p { font-size: 1rem; line-height: 1.65; color: var(--color-brand-muted); margin: 0; }

  /* ============ DELIVERABLES ============ */
  .sp-cross { background: var(--ivory); padding: clamp(4rem, 9vw, 8rem) 0; }

  .sp-next-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: clamp(1.2rem, 2.5vw, 1.6rem); max-width: 1000px; margin-inline: auto; }
  .sp-next-card { display: flex; flex-direction: column; background: var(--color-brand-surface); border: 1px solid rgba(31, 59, 44, 0.1); border-radius: 1.4rem; padding: clamp(1.5rem, 3vw, 2rem); transition: transform 0.4s cubic-bezier(0.22, 1, 0.36, 1), box-shadow 0.4s ease, border-color 0.4s ease; }
  .sp-next-card:hover { transform: translateY(-6px); box-shadow: 0 26px 50px -20px rgba(31, 59, 44, 0.28); border-color: rgba(176, 141, 76, 0.55); }
  .sp-next-kicker { font-family: var(--font-body); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase; color: var(--brass); }
  .sp-next-card h4 { font-family: var(--font-heading); font-weight: 600; font-size: 1.5rem; color: var(--color-brand-text); margin: 0.55rem 0 0.6rem; }
  .sp-next-card p { font-size: 0.95rem; line-height: 1.65; color: var(--color-brand-muted); margin: 0 0 1.1rem; }
  .sp-next-card .text-link { margin-top: auto; color: var(--color-brand-primary); }

  /* ============ FAQ (two-column) ============ */
  .sp-faq { background: var(--color-brand-surface); padding: clamp(4rem, 9vw, 8rem) 0; }
  .sp-faq-list { display: flex; flex-direction: column; max-width: 820px; margin-inline: auto; }
  .sp-faq-item { border-top: 1px solid rgba(31, 59, 44, 0.14); background: var(--ivory); border-radius: 0 0 1rem 1rem; }
  .sp-faq-item:first-child { border-radius: 1rem 1rem 0 0; }
  .sp-faq-item:last-child { border-bottom: 1px solid rgba(31, 59, 44, 0.14); border-radius: 0 0 1rem 1rem; }
  .sp-faq-item + .sp-faq-item { margin-top: 0.6rem; }
  .sp-faq-num { font-family: var(--font-heading); font-weight: 600; color: var(--brass); margin-right: 0.9rem; }
  .sp-faq-item summary { list-style: none; cursor: pointer; display: flex; align-items: center; gap: 1rem; padding: 1.4rem 1.2rem; font-family: var(--font-heading); font-weight: 600; font-size: clamp(1.05rem, 1.8vw, 1.25rem); color: var(--color-brand-text); }
  .sp-faq-item summary::-webkit-details-marker { display: none; }
  .sp-faq-item summary::after { content: '+'; font-family: var(--font-body); font-weight: 400; font-size: 1.6rem; color: var(--brass); transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1); margin-left: auto; }
  .sp-faq-item[open] summary::after { transform: rotate(45deg); }
  .sp-faq-a { padding: 0 1.2rem 1.4rem; }
  .sp-faq-a p { font-size: 0.98rem; line-height: 1.7; color: var(--color-brand-muted); max-width: 68ch; margin: 0; }

  /* ============ FINAL CTA (dark) ============ */
  .sp-final { background: linear-gradient(180deg, var(--forest-light) 0%, var(--color-forest) 100%); color: var(--ivory); padding: clamp(4.5rem, 10vw, 8rem) 0; position: relative; overflow: hidden; }
  .sp-final::before { content: ''; position: absolute; inset: 0; pointer-events: none; background: radial-gradient(48% 70% at 22% 30%, rgba(228, 181, 77, 0.14), transparent 70%); }
  .sp-final-inner { text-align: center; position: relative; z-index: 1; }
  .sp-final-title { font-family: var(--font-heading); font-weight: 500; font-size: clamp(2.4rem, 6vw, 4.4rem); line-height: 1.04; letter-spacing: -0.025em; color: #FFFFFF; text-shadow: 0 2px 24px rgba(20, 38, 27, 0.4); margin: 1.1rem 0 0; max-width: 24ch; margin-inline: auto; text-wrap: balance; }
  .sp-final-title em { font-style: italic; color: #E3C29A; }
  .sp-final-sub { font-size: 1.08rem; line-height: 1.7; color: rgba(255, 255, 255, 0.95); max-width: 52ch; margin: 1.5rem auto 0; text-shadow: 0 1px 18px rgba(20, 38, 27, 0.4); }
  .sp-final .section-cta { margin-top: clamp(2.2rem, 5vw, 3rem); }

  /* ============ REVEAL ============ */
  [data-reveal] { opacity: 0; transform: translateY(36px); transition: opacity 0.8s cubic-bezier(0.22, 1, 0.36, 1), transform 0.8s cubic-bezier(0.22, 1, 0.36, 1); transition-delay: var(--d, 0s); }
  [data-reveal='right'] { transform: translateX(48px); }
  [data-reveal='left'] { transform: translateX(-48px); }
  [data-reveal].in { opacity: 1; transform: none; }

  /* ============ RESPONSIVE ============ */
  @media (max-width: 900px) {
    .sp-hero-grid { grid-template-columns: 1fr; }
    .sp-first-row { grid-template-columns: repeat(2, 1fr); }
    .sp-id-row { grid-template-columns: 1fr; }
    .sp-id-row--rev { direction: ltr; }
    .sp-id-row--rev .sp-id-body { align-items: flex-start; text-align: left; }
    .sp-approve-grid { grid-template-columns: 1fr; }
    .sp-test-grid { grid-template-columns: 1fr; }
    .sp-test-head { position: static; }
  }
  @media (max-width: 640px) {
    .sp-id-cols { grid-template-columns: 1fr; gap: 1.1rem; }
    .sp-signoff { grid-template-columns: 1fr; }
    .sp-next-grid { grid-template-columns: 1fr; }
    .sp-hero-cta .btn { width: 100%; }
  }
</style>

<script>
  const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const hasIO = 'IntersectionObserver' in window;
  const revealEls = document.querySelectorAll('[data-reveal]');

  if (!reduce && hasIO) {
    const revealIO = new IntersectionObserver((entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) { e.target.classList.add('in'); revealIO.unobserve(e.target); }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
    revealEls.forEach((el) => revealIO.observe(el));
  } else {
    revealEls.forEach((el) => el.classList.add('in'));
  }
</script>
